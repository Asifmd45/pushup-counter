# pushup-counter
A real-time push-up counter application that uses computer vision and pose detection to automatically count push-ups and provide form feedback during exercise.

pushup counter/
├── main.py                    # Main application file
└── requirements.txt          # Python dependencies
